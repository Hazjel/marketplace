$kind: http-request
url: "{{base_url}}/withdrawal"
method: POST
headers:
  Accept: application/json
body:
  type: formdata
  content:
    - type: text
      key: amount
      value: "100000"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 3000
