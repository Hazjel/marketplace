$kind: http-request
url: "{{base_url}}/register-store"
method: POST
headers:
  Accept: application/json
body:
  type: formdata
  content:
    - type: text
      key: name
      value: Toko Saya
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 7000
