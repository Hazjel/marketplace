$kind: http-request
url: "{{base_url}}/store/1/verified"
method: POST
headers:
  Accept: application/json
body:
  type: formdata
  content:
    - type: text
      key: is_verified
      value: "true"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 13000
