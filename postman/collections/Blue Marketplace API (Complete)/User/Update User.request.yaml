$kind: http-request
url: "{{base_url}}/user/1"
method: PUT
headers:
  Accept: application/json
body:
  type: formdata
  content:
    - type: text
      key: name
      value: Updated Name
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 3000
